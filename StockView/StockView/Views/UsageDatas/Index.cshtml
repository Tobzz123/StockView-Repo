@model IEnumerable<StockView.Models.UsageData>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StockBuyers)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StockSellers)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StockBuyers)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StockSellers)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UsageId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UsageId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UsageId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
